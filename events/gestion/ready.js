const Discord = require("discord.js");

module.exports = (client) => {
	console.log(`✅ Bot prêt: ${client.user.username}`);
	
	// Version simplifiée sans les compteurs qui utilisaient quick.db
	// Si vous avez besoin de réactiver cette fonctionnalité plus tard,
	// il faudra mettre à jour les appels à quick.db pour la v9
} 